# Task 5 – Python Docker BusyBox

## Description
This task runs a BusyBox container using Python's docker package.
- Keeps the container running using `sleep`.
- Executes a command inside the container to retrieve the hostname.
