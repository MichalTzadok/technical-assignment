# Task 6 – Coordinate Conversion (DD → DMS)

## Description
This task converts geographic coordinates from **Decimal Degrees (DD)** format
to **Degrees, Minutes, Seconds (DMS)** format.

The program receives a JSON structure that contains coordinates in DD format
and converts them into DMS while keeping the direction (N, S, E, W).
