# Task 8 – RGB Histogram with NumPy and Matplotlib

This task implements a Python script that computes and plots RGB histograms 
from an input image using **NumPy** and **Matplotlib**.


## How to run  
```bash
pip install numpy matplotlib opencv-python
python main.py
```
